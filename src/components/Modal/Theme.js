import { createTheme } from '@mui/material';

export const theme = createTheme({
  palette: {
    primary: {
      main: '#000000',
      light: '#FC842D',
    },
    secondary: {
      main: '#000000',
    },
    buttonColor: {
      main: '#FC842D',
    },
  },
});
